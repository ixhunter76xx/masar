-- ═══ كبح تخمين كلمات المرور ═════════════════════════════════════════
--
-- لم يكن في المنصة أي حدّ لمعدّل المحاولات: `/login` يقبل عددًا غير
-- محدود من المحاولات، وكل واحدة تُشغّل `bcrypt.compare` — فهي كذلك
-- بابٌ لاستنزاف المعالج لا لتخمين الكلمة وحده.
--
-- العدّاد عمودان على الحساب لا جدول منفصل ولا ذاكرة عملية:
--   • الذاكرة تضيع مع كل نسخة باردة على نشرٍ بلا خادم دائم، فيُصفَّر
--     الحظر للمهاجم تلقائيًا.
--   • الجدول المنفصل يلزم لو أردنا الحدّ **بعنوان الشبكة** أيضًا، وهو
--     ما يحتاج بنية عند الحافّة — خارج نطاق هذه المرحلة وموثّق.
--
-- **إضافي بالكامل**: عمودان بقيم افتراضية، لا يمسّان صفًّا قائمًا.
-- ═══════════════════════════════════════════════════════════════════

ALTER TABLE "users" ADD COLUMN "failedLoginAttempts" INTEGER NOT NULL DEFAULT 0;
ALTER TABLE "users" ADD COLUMN "lockedUntil" TIMESTAMP(3);
