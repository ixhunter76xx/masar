# تسليم الحالة — 2026-08-30

> يُقرأ مع [`CLAUDE.md`](CLAUDE.md) لا بدلًا منه. هذا الملفّ **يقول أين
> نقف الآن وما ينتظر**؛ وذاك يحمل التعليل الكامل والفخاخ المسجَّلة.

---

## ١ · أين نقف

**الإنتاج `masar-bh.com` على `c2fa44e`**، و`master` و`masar-port`
متطابقان معه ومدفوعان إلى `origin`. الشجرة نظيفة.

آخر أربعة إيداعات:

```text
c2fa44e  perf: أقلّ دورات إلى القاعدة، وحزمةٌ أخفّ لصفحتَي الدخول
c289515  feat(design): واجهة مباشرة، محطّات أفقية، ومسارٌ بالفصول والملفّات
4e14822  chore: زامِن deno.lock مع @prisma/adapter-neon
bec8f65  docs: حدّث هوية المستودع ووثائقه إلى «مسار»
```

**الحرّاس الخمسة خضراء**، و`ALL FEATURES PASS`، و`typecheck` نظيف.

---

## ٢ · العائق الوحيد أمام تشغيلٍ حقيقيّ

```
دروس جاهزة داخل باقةٍ منشورة = ٠
```

المتجر يبيع، ومحتواه خلف الدفع **فارغ**. `ITCS107` منشور بثلاث باقات
وصفر دروس جاهزة، و`ARAB110` فيه درسٌ جاهز واحد لا ينتمي إلى باقة.

**السبب سلسلة، أوّلها ليس برمجيًّا:**

1. **CORS على دلو R2** — أضِف `https://masar-bh.com` و
   `https://www.masar-bh.com` إلى سياسة CORS للدلو `hisab-media` من لوحة
   Cloudflare (القائمة الكاملة في README §٢). **لا يُضبط بسكربت**: رمز
   التطبيق يملك صلاحيات الكائنات لا إعدادات الدلو، و`GetBucketCors`
   يردّ `AccessDenied`.
2. **رفع الدروس وربطها بالباقات** — عملُ محتوًى بعد فتح CORS.

⚠ وإذا فشل الرفع فشخّصه **من طرفية المتصفّح لا من الاستثناء**: رفضُ CSP
ورفضُ CORS كلاهما يظهر `xhr.onerror` عاريًا، ولا يُفرَّق بينهما إلا في
الطرفية. (فخٌّ كلّف جلستين، مسجَّل في `CLAUDE.md`.)

---

## ٣ · جولة تصميمٍ مفتوحة — القرار عند المالك

المالك طلب **إعادة تصميم كاملة** بعد أن استعمل الموقع، ووصف الحالي بأنه
«بلا روح، ساكن بشكل سيء». والعمل يجري **خارج المستودع** عبر أداة تصميم:

- ملفّ السياق المُسلَّم: [`design/masar-redesign-brief.md`](design/masar-redesign-brief.md)
  — أحد عشر قسمًا: الهوية بقيمها الست عشرية، خريطة الصفحات، النموذج
  التجاري بأسعارٍ حقيقية، قيود RTL والأداء.
- **لم يُنفَّذ منه شيءٌ في الشيفرة بعد.** ما في `master` هو نتيجة جولة
  2026-08-30 (المحطّات الأفقية والمشهدان والمسار بالفصول)، لا إعادة
  التصميم.

**الدرس المستفاد من جولات الأداة** (يُعاد إن استُؤنفت): وصفٌ عامّ
(«اجعله أكثر مباشرة») لا يكفي؛ والمواصفة الحرفية تُنتج **نسخًا** من
المرجع المنافس — نصوصه وأزراره. الحدّ الصحيح: يُقتبَس **التخطيط
والكثافة** فقط، وكلُّ نصٍّ وفعلٍ من هوية مسار ونموذجها.

⚠ وأوضحُ خطأٍ وقعت فيه الأداة: أضافت زرّ «التسجيل والاشتراك» إلى بطاقة
الكتالوج. **مسار لا تبيع من الشبكة** — البطاقة تقود إلى صفحة المقرر،
وهناك الباقات الثلاث. زرٌّ واحد في البطاقة لا اثنان.

---

## ٤ · مقبولٌ ومؤجَّل بقرار المالك — لا يُعاد طرحه كعيب

- **الدفع اليدويّ** عبر واتساب/بنفت — قرارٌ واعٍ حتى ترخيصٍ خفيف
  (سجل تجاريّ افتراضيّ أو رخصة عمل حرّ). **لا تُضَف بوّابة دفع** قبل
  حسم الترخيص.
- **`npm audit`: ٧ عالية** في `sharp`/`postcss` عبر `next` — غير قابلة
  للوصول هنا (`next.config.ts` بلا `remotePatterns`، والملفّات المرفوعة
  تُقدَّم من R2 برابطٍ موقَّع لا عبر `next/image`). إصلاحها ترقية Next
  كاسرة.
- **لا تحديد معدّل للتسجيل**، و**التسجيل يكشف وجود البريد** — مقايضة
  استعمالٍ متروكة لك.
- **٢٠٠ بدل ٤٠٤** في المنطقة المحمية — أجّله المالك صراحةً. وأثرُه على
  الاختبار: **رمزُ الحالة لا يقول إن الوصول مُنع**؛ قِس بطول الاستجابة
  مقابل معرّفٍ مفبرك.
- **التحليلات** لم تبدأ. `reportError` للأعطال لا للاستخدام.
- **ثلاثة بنودٍ قانونية** في `/legal/terms` عند أضعف التزام بانتظار قرار.

---

## ٥ · أكبر مكسب أداءٍ متبقٍّ — وهو قرار بنيةٍ لا شيفرة

**دورةٌ واحدة إلى Neon = ‏٢٤٢ms** (مقيسة، وسيط ٧ عيّنات). وزمنُ أي صفحةٍ
محميّة = عددُ دوراتها × ٢٤٢. القاع ‏٤٩٠ms (دورتان: `getLiveUser` ثمّ
`getNavCounts` التي تحتاج الدور منها — تسلسلٌ حقيقيّ).

القاعدة في `us-east-2` والمستخدم في البحرين. **نقلُ مشروع Neon إلى
إقليمٍ أقرب يقسم كل رقمٍ على اثنين تقريبًا بلا سطرٍ واحد من التعديل.**

⚠ ولا يُطوى `getLiveUser` مع `getNavCounts` لتوفير دورة: طيُّهما يعني
أخذ الدور من الرمز، وهو بالضبط ما أغلقته جولة الأمن.

---

## ٦ · أوامر التحقّق

```powershell
npm run typecheck
npm run build:local
npx tsx --tsconfig tsconfig.script.json scripts/brand-regression.mts
npx tsx --tsconfig tsconfig.script.json scripts/security-regression.mts
npx tsx --tsconfig tsconfig.script.json scripts/nav-feedback-regression.mts
npx tsx --tsconfig tsconfig.script.json scripts/motion-shell-regression.mts
npx tsx --tsconfig tsconfig.script.json scripts/course-transition-regression.mts
npx tsx --tsconfig tsconfig.script.json scripts/features.mts   # يحتاج :3100 حيًّا
npx tsx --tsconfig tsconfig.script.json scripts/probe.mts      # يحتاج :3100 حيًّا
```

الحرّاس الخمسة ساكنة (بلا خادمٍ ولا قاعدة) وتعمل في CI. وكلٌّ منها يحرس
عطلًا **صامتًا** — يمرّ فيه البناء و`tsc` والشاشات تعمل — وكلّ ثابتٍ
فيها أُثبت سقوطه بكسره عمدًا ثمّ إعادته.

---

## ٧ · فخاخٌ تُوقِع فورًا

1. **لا تُعِد `AUTH_URL`.** `trustHost: true` يشتقّ الأصل من الطلب،
   وتثبيتُه يكسر أحد النطاقين.
2. **لا تشغّل `next dev` و`next start` معًا** — `.next` مشترك،
   والنتيجة `ChunkLoadError` صامت. العلاج: أوقف الاثنين، `rm -rf .next`،
   ثم أعد التشغيل.
3. **الهجرات تحتاج `DIRECT_URL`** (بلا `-pooler`). وإن علق القفل:
   `PRISMA_SCHEMA_DISABLE_ADVISORY_LOCK=1 npx prisma migrate deploy`.
4. **قاعدةٌ واحدة لكل شيء** — لا بيئة تجريب. كل هجرةٍ تمسّ الإنتاج.
5. **الدفع إلى `master` نشرٌ للإنتاج.**
6. **`markOrderPaid` و`refundOrder` كاتبان وحيدان** لأي وصولٍ مصدره
   شراء. لا تُضِف مسارًا ثانيًا يمنح الوصول.
7. **دالّة تفويضٍ تشتقّ هويتها بنفسها** — معامل `role` عليها هو العيب.
