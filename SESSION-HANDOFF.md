# Session Handoff — 2026-08-22

## 🚨 عملٌ واحد ينتظرك — ودقيقةٌ تكفيه

**الهوية الجديدة جاهزةٌ ومُودَعة ومدفوعة إلى `master`، ولم تُنشر.**
Netlify حجبت النشر لسببٍ **محاسبيّ لا برمجيّ**:

```
Skipped due to account credit usage exceeded
netlify deploy --prod → JSONHTTPError: Forbidden
```

الحساب: فريق `SayedAli` · خطّة **Free** · **بلا بطاقة** · الرصيد
مستنفد. البناء لم يفشل — لم يُشغَّل أصلًا. وكل نشرةٍ قبله كانت `ready`.

**الموقع يعمل** (`masar-bh.com` تردّ ٢٠٠) لكنه يقدّم البناء السابق، أي
**الشعار القديم**. المقيس: `icon-512.png` المنشور ٢٨٩٢٣ بايت، والجديد
٣٨٢٤٧.

**الحلّ:** أضِف وسيلة دفع في `app.netlify.com` ← Team `SayedAli` ←
Billing (أو انتظر تجدّد الرصيد الشهريّ)، ثم:

```bash
npx netlify deploy --build --context dev --prod
```

لا حاجة إلى أي تعديل — الإيداعان `050b72c` و`a2c05af` جاهزان على
`master`. أو أعِد تشغيل آخر نشرة من لوحة Netlify.

---

## 🎨 الهوية الجديدة — بُدِّلت في كل موضع، وصار لها حارس

الشعار الجديد (خطٌّ عربيّ يمشي على طريق يحمل خرّيجًا) حلّ محلّ الرمز
الهندسيّ القديم في **الملفّات السبعة كلّها**.

**ولماذا فاتني هذا في جولة الإطلاق:** الهوية سبعةُ ملفّات لا ملفّ واحد،
وثلاثةٌ منها (`icon-maskable-*`, `apple-touch-icon`) لا تُرى إلا على
شاشة هاتفٍ مثبَّتٍ عليه التطبيق. فكلٌّ منها سليمٌ في ذاته وإنما هو
**قديم** — والقِدَم لا يُرى بالنظر إلى ملفٍّ واحد، بل بمقارنة الملفّات
بعضها ببعض. ولذلك صار المصدر واحدًا والاشتقاق آليًّا:

```bash
npx tsx --tsconfig tsconfig.script.json scripts/generate-icons.mts assets/logo-source.png
```

يُخرج السبعة ويكتب `assets/brand-lock.json` ببصماتها. **لا يُحرَّر أصلٌ
بيد** — `brand-regression` يسقط إن بُدِّل ملفٌّ وحده أو تُرك وحده.

### `SITE.brandVersion` — بدونها لا يرى أحدٌ الشعار الجديد

النشر وحده لا يكفي؛ ثلاث ذاكرات تُبقي القديم: المتصفّح (الملفّ بعنوانه)،
و**واتساب** (`og:image` لكل رابطٍ شورك — وهي قناة البيع الأولى)، ونظام
الهاتف (أيقونة التطبيق المثبَّت). فالنسخة تُلحق بكل رابط أصل، من ثابتٍ
واحد.

### شاشة إقلاع التطبيق المثبَّت

`src/components/brand/PwaLaunch.tsx` — تظهر عند فتح «مسار» من أيقونته
على الشاشة الرئيسية، لا في المتصفّح.

- **شرطها في CSS لا جافاسكربت**: `matchMedia` لا يقع إلا بعد الترطيب
  فيومض المحتوى قبلها. فهي **مكوّن خادم بصفر JS**.
- `pointer-events: none` طوال عمرها — لا تحجب النقر أبدًا.
- الطريق يُمدّ من **يمين** السطر (RTL)، مقيسًا: `transform-origin: 208px 1px`.
- **عند تخفيض الحركة تُسقَط كاملةً** — كلّ قيمتها في الحركة.

المقيس في المتصفّح: ‏٤٢٠ms الشعار مكتملٌ والطريق ممدود · ‏١٠٠٠ms السطر
ظاهر · ‏١٥٠٠ms `opacity: 0` و`visibility: hidden`.

### الحارس — ستّة أعطالٍ أُثبت سقوطه بها

`scripts/brand-regression.mts`: أصلٌ مبدَّل وحده · أصلٌ يتيم · رابطٌ بلا
`?v=` · `PwaLaunch` غير مركَّب · لونٌ يخالف `--color-ink` · شرط
`display-mode` غائب. **وأُضيف مع الحرّاس الأربعة الباقين إلى CI**، فلن
يمرّ هذا الصنف صامتًا مرّةً أخرى.

وحُذف `public/logo-masar-mark.png` — ١٣٤ ك.ب من هويةٍ متقاعدة بصفر مراجع.

### حالة التحقّق

الحرّاس الخمسة خضراء · `typecheck` نظيف · `ALL FEATURES PASS` ·
**البصمة المنطقية `b008c722…` كما هي** (لم تُمسّ الخلفية إطلاقًا).

---

## ما يلي ذلك (من الجلسات السابقة، ما زال قائمًا)

- **CORS على دلو R2**: أضِف `https://masar-bh.com` و`https://www.masar-bh.com`
  في لوحة Cloudflare ← R2 ← `hisab-media` ← Settings ← CORS. بدونه يفشل
  رفع الفيديو من النطاق الجديد. لا يُضبط بسكربت (رمز التطبيق يملك
  صلاحيات الكائنات لا إعدادات الدلو).
- **المحتوى**: صفر دروس جاهزة داخل باقةٍ منشورة — أي أن المشتري اليوم
  لا يجد ما يشاهده. بعد فتح CORS: ارفع درسًا واربطه بباقة.
- **مراجعة `/legal/terms`**: ثلاثة بنودٍ عند أضعف التزام.

---

<details>
<summary>الحالة كما سُجّلت في 2026-08-20 (تاريخيّة)</summary>

# Session Handoff — 2026-08-20

## 🔒 جولة الأمن — ثغرتان مؤكَّدتان، أُغلقتا ونُشرتا

الإيداع `c410440` منشورٌ على `masar-bh.com`. التفصيل الكامل بالأدلّة
في CLAUDE.md ‹The security audit›. الخلاصة:

**١ · التفويض كان يُقرأ من ادّعاء الرمز لا من القاعدة.** تعطيلُ حساب
وتصفيرُ كلمة مرور كانا يُنهيان الجلسة **على الصفحات وحدها**، بينما
تبقى أربعة مسارات API ونحو عشرين إجراء إدارة مقرر مفتوحة. صارت
`canManageCourse(courseId)` تقرأ المستخدم الحيّ ولا تقبل شيئًا من
المستدعي.

**٢ · تحويلٌ مفتوح بعد الدخول.** `next=//evil.com` كان يقذف المستخدم
خارج الموقع **بعد نجاح تسجيل الدخول**. `safeNextPath` يقبل مسارًا
داخليًّا واحدًا، ويُطبَّق على الخادم.

**حارسٌ جديد:** `scripts/security-regression.mts` — أربعة ثوابت، كلٌّ
منها مُثبَتٌ أنه يسقط.

### ما بقي مقبولًا بقرارٍ لا بغفلة

`npm audit` سبعُ ثغراتٍ عالية كلّها في `sharp`/`postcss` عبر Next،
وغيرُ قابلةٍ للوصول هنا (لا `remotePatterns`، والمرفوعات تُقدَّم من R2
برابطٍ موقّع لا عبر `next/image`)، وإصلاحُها ترقيةٌ كاسرة. ولا تحديد
معدّل على التسجيل (مسجَّل سابقًا كقرار)، وكشفُ وجود البريد عند
التسجيل، وحجمُ الرفع مُصرَّحٌ به لا مُلزَم.


## ✅ نُشر إلى الإنتاج — الجولات الأربع كلها

```text
origin/master  ←  masar-port  (تقديمٌ سريع، ١٧ إيداعًا)
```

**ما قبل النشر كان الإنتاج على `205f61c`.** هذا النشر يحمل: لوحة
الإدارة الكاملة، ومشهدَي الكليات، وقواعد العدد العربي، وطبقة الاعتراف
بالنقرة، وإصلاحَي التنقّل السريع، وتهيئة النطاق.

### ✅ `AUTH_URL` — حُذف، والعطل كان حيًّا على الإنتاج فعلًا

لحظةَ عمل `masar-bh.com` ظهر ما تنبّأ به الملفّ: **كل مسارٍ محميّ
يقذف الزائر إلى النطاق القديم.**

```text
masar-bh.com/dashboard → https://hisab-lms.netlify.app/login?next=…
```

الآلية: `reqWithEnvURL` في next-auth يعيد كتابة أصل الطلب من
`AUTH_URL` **قبل أن يراه middleware** — فـ`new URL("/login", req.nextUrl)`
يعطي المضيف القديم مهما كان الطلب.

حُذف المتغيّر من لوحة Netlify (منع المصنّفُ أمرَ الطرفية، ففُعل من
الواجهة). **ولا يكفي الحذف وحده**: الحزمة المنشورة تحمل القيمة من
وقت البناء، فلزم إعادة بناء. بعدها:

| | قبل | بعد |
|---|---|---|
| `masar-bh.com/dashboard` | → `hisab-lms.netlify.app/login` | → `masar-bh.com/login` ✓ |
| `hisab-lms.netlify.app/dashboard` | → نفسه | → نفسه ✓ |
| جلسة مدير حقيقية على النطاقين | — | ٢٠٠ وزرّ الخروج حاضر على الاثنين ✓ |

> ⚠ **لا تُعِده أبدًا.** `trustHost: true` مضبوط، ولا موضع في الشيفرة
> يقرأه. وتثبيتُه على أيّ نطاق يكسر الآخر — وهذا مسجَّل الآن في
> CLAUDE.md و`.env.example` معًا.

### ⏳ البند الوحيد الباقي — CORS على R2

أضِف في لوحة Cloudflare ← R2 ← `hisab-media` ← Settings ← CORS:
`https://masar-bh.com` و`https://www.masar-bh.com`. القائمة الكاملة
في README §٢.

بدونه **يفشل رفع الفيديو من النطاق الجديد** بـ`xhr.onerror` عارٍ لا
يميّزه شيء عن رفض CSP. ولا يُضبط بسكربت: رمز التطبيق يملك صلاحيات
الكائنات لا إعدادات الدلو (`GetBucketCors` → `AccessDenied`).

## ✅ الجولة الرابعة — نطاق `masar-bh.com` (`21f7748`)

اشتراه المالك وجعله الأساسيّ في Netlify، وDNS ينتشر. **المصدر كان
خاليًا من أي نطاقٍ مكتوب بيده** — التوجيه من `req.nextUrl`، وCSP
كلّها `'self'`. فما احتاج تغييرًا: الروابط المطلقة، والفهرسة،
وAUTH_URL.

| ما تغيّر | لماذا |
|---|---|
| `SITE.url` | أصلٌ واحد للروابط المطلقة وحدها — لا توجيه ولا جلسة |
| `metadataBase` + Open Graph | رابط المقرر يُشارَك عبر واتساب وكان يُشارَك عاريًا |
| `canonical` لصفحة المقرر | النطاق يستجيب بـ`www` وبدونه — بلا معياريّ يقسم جوجل الوزن |
| `robots.ts` | كان يمنع الفهرسة كاملةً؛ صار يفتح الكتالوج ويمنع ما خلف الدخول |
| `sitemap.ts` ‹جديد› | يقرأ المخزون العام نفسه فيتبع المنشور تلقائيًا |
| `middleware.ts` | ⚠ مسٌّ مقصود — انظر أدناه |
| README · CORS | كان يذكر `lms.hisab.edu` وهو نطاقٌ من الهوية القديمة |

### ⚠ عيبٌ كشفه هذا العمل: ملفّا الزواحف كانا محجوبين

`curl /robots.txt` كان يعيد `/login?next=%2Frobots.txt`. المُطابِق
يستثني `sw.js` والبيان والأيقونات ولم يستثنِهما. أي أن `robots.txt`
**لم يصل إلى زاحفٍ قطّ**، ولم يظهر الأثر لأن مضمونه كان «امنع كل
شيء» والصفحات محجوبة أصلًا — فالخطأ والصواب يعطيان النتيجة نفسها.

> **البصمة تغيّرت بمسٍّ مقصود** لسطرين في `PUBLIC_EXACT`:
> `789b6e08e364bf8769c9e7550de7d386dc7defef67c6e0c357d5a343d96a2c0f`
> والمِجَسّ مطابقٌ للأساس بعدها. ولا يكشف الملفّان شيئًا: أحدهما
> قائمةُ مسارات، والآخر لا يحوي إلا المقررات المنشورة.

### ⏳ بندان **لم أنفّذهما** — يحتاجان يد المالك

**١ · `AUTH_URL` على Netlify — وهذا هو الذي يكسر الدخول.**
مثبَّتٌ على `hisab-lms.netlify.app` بنطاق All. تثبيتُه على أيّ نطاق
يكسر الآخر. والصواب ألّا يُعرَّف (`trustHost: true` مضبوط، ولا موضع
في الشيفرة يقرأه):

```bash
npx netlify env:unset AUTH_URL
```

مقيسٌ محليًّا بترويسة `Host` مصطنعة على النطاقين معًا: تحويلٌ نسبيّ
`/login?next=…`، و`csrf` يُصدر رمزًا بلا `UntrustedHost`، و`/login`
‏٢٠٠. حاولتُ تنفيذه فمنعه مصنّف الصلاحيات — وهو تغييرُ بيئةِ إنتاج.

**٢ · CORS على دلو R2 في لوحة Cloudflare.** أضِف `https://masar-bh.com`
و`https://www.masar-bh.com`. بدونه **يفشل رفع الفيديو من النطاق
الجديد**، بـ`xhr.onerror` عارٍ لا يميّزه شيء عن رفض CSP — وهو ما
كلّف جلستين من قبل. القائمة الكاملة في README §٢.

> ولا يمكن ضبطه بالسكربت: رمز التطبيق يملك صلاحيات الكائنات لا
> إعدادات الدلو، و`GetBucketCors` يعيد `AccessDenied` (مسجَّل).

### وبعد انتشار DNS، تحقّق بهذه

```bash
curl -sI https://masar-bh.com/courses | head -1        # 200
curl -s  https://masar-bh.com/robots.txt | head -3     # Allow: /courses
curl -s  https://masar-bh.com/sitemap.xml | head -3    # masar-bh.com فيها
curl -sI https://masar-bh.com/dashboard | grep -i loc  # /login نسبيًّا
```

## ✅ الجولة الثالثة — عطلان بلّغ عنهما المالك، وكلاهما مُصلَح



بلاغُه: «عند التنقل بكثرة وبسرعة في الدراسة يبدأ بعض المحتوى لا يظهر…
وفي الإعدادات عندما أبدّل بين الطلبات والمقررات يختفي المحتوى كاملًا،
والخط المتحرك لا يعمل بالشكل المطلوب». والبلاغ صحيح في شقّيه.

```text
e7a2c29  fix(admin):  ارفع شريط تبويبات الإدارة إلى تخطيطٍ فوق المحتوى
368b430  fix(motion): احذف AnimatePresence من المنطقة المحمية
432409e  fix(a11y):   وسّع مساحة إصابة رابطَي الرأسية العامّة إلى ٤٤px
36da4fc  test(motion): احرس العطلين
```

### العطل الأول — شريط تبويبات الإدارة يختفي

**لم يكن لمسار `/settings` تخطيطٌ أصلًا.** كل صفحةٍ من الستّ كانت
تصيّر `AdminTabs` بنفسها داخل `AppPage` خاصّ بها. فالتنقّل يفكّك
الصفحة ومعها الشريط، ويحلّ محلّها `loading.tsx` الذي يرسم هيكل
المحتوى ولا يرسم شريطًا. وللسبب نفسه كان المؤشّر لا يستطيع الانزلاق:
`layoutId` يصل بين موضعين لعنصرٍ **باقٍ**.

العلاج: بنية `learn/[courseId]/(tabs)` نفسها — تخطيطٌ يحمل الشريط،
وصفحاتٌ عارية تحته، و`loading.tsx` مسؤوليتُه جسم التبويب وحده،
ومفتاح انتقالٍ مشترك في `appPageTransitionKey`.

| مقيسًا بخنق معالجٍ ١٢× | قبل | بعد |
|---|---:|---:|
| إطارات الهيكل ومعها التبويبات | ٠ من ٣٤٣ | **١٤٢ من ١٤٢** |
| أطول اختفاءٍ للشريط | **٢٨٦٨ms** | **٠ms** |
| انزلاق المؤشّر | قفزة بعد غياب ٩٦٦ms | `1085→1012` بنابضه |
| خطأ React ‏#418 | موجود | **زال** |

### العطل الثاني — محتوًى قديم تحت عنوانٍ جديد ⚠ أحدثتُه أنا

المسار `/learn`، والشريط الجانبي يعلّم «مقرراتي»، والشاشة تعرض
**تبويبات الإدارة ومحتواها**. أثبتَه قياسٌ على شجرتَي عملٍ متوازيتين:

```text
be17fb4 (قبل عملي كلّه)     0/4
بعد إصلاح تبويبات الإدارة    4/4   ← أنا
بعد إصلاح PageTransition     0/4
```

**الآلية:** `mode="wait"` يُبقي النسخة الخارجة حتى تُعلن انتهاء
خروجها. وحدُّ Suspense الجديد الذي أضفتُه (`settings/(tabs)/loading.tsx`)
قد لا يُعلنه أبدًا. فيبقى الابن القديم، و`FrozenRouter` يؤدّي عمله
بأمانة فيخدمه سياقَه المجمَّد.

**والعلاج حذفٌ لا تبديل:** `APP_PAGE.exitTransition` مدّته صفر عمدًا،
فلا خروجَ يُنتظَر — أي أن `AnimatePresence` في الوضع الثابت لا تشتري
شيئًا. ولا يُستبدل بـ`popLayout` (يقتل الترطيب تحت Suspense).

> **فائدة تبعية:** في الوضع الثابت صار `mountedKey` مساويًا دائمًا
> لمفتاح المسار، فـ`isExiting` لا يصير صحيحًا أبدًا — أي أن
> `FrozenRouter` يمرّر السياق **الحيّ** دومًا. وهذا أمتنُ لـ
> `router.refresh()` مما كان. وتحقّقتُ منه فعلًا: تعطيل حسابٍ
> تجريبيّ ثم إعادته، والزرّ يتبدّل في الوثيقة نفسها.

### ⚠ ثلاثة دروس منهجية من هذه الجولة

1. **الكاشف الأول كان يكذب.** عددتُ «إطارًا فارغًا» كل إطارٍ بلا
   `main#main` — و`loading.tsx` لا يصيّر `main#main` أصلًا. فأعطى
   ٣٢٧ من ٥٨٩ «فراغًا» وهو هيكلُ تحميلٍ يؤدّي عمله. **ميّز الهيكل
   من الفراغ قبل أن تعدّ.**
2. **تشخيصي الأول كان خاطئًا.** ظننتُ `layoutId` سببَ الاختفاء.
   والمقارنة على شجرتين متوازيتين نقضته (١٠٢ مقابل ١٠٣). **قارن
   بالأساس قبل أن تتّهم آخر ما كتبت.**
3. **`getBoundingClientRect` لا يقيس مساحة الإصابة.** الزائف
   `::after` يوسّعها بلا أن يظهر فيه. قِسها بالنقر
   (`elementFromPoint` صعودًا ونزولًا) وإلا أعطيتَ إنذارات كاذبة.

### فحص الأجهزة — أربعة أجهزة × سبع شاشات

٣٧٥ · ٣٩٠ · ٤١٢ · ٧٦٨ بمحاكاة جهازٍ حقيقية (لمس + نسبة بكسل):
**بلا تمرير أفقي في أيٍّ من الثمانٍ وعشرين شاشة**، واللوحة المنسحبة
بمؤشّرٍ واحد لكل قائمة، وبلا أخطاء طرفية.

ووجد الفحص هدفَ لمسٍ حقيقيًّا واحدًا دون الحدّ: رابط الحساب/الدخول
في الرأسية العامّة (٣٤px إصابةً). صار ٤٤px بـ`tap-44` بلا تغيير شكل.

### السرعات المقيسة على جهاز المالك

| | TTFB | تفاعليّ | اكتمال |
|---|---:|---:|---:|
| `/courses` (عامّ، مخزَّن ٦٠ث) | **١٧ms** | ٥٢ms | ١٩١ms |
| `/dashboard` (يقرأ القاعدة) | **٤٥٥ms** | ١١٤٩ms | ١١٥٦ms |

الفارق كلّه رحلاتُ Neon في `us-east-2` من البحرين — حدٌّ جغرافيّ
مسجَّل في `PORT-HANDOFF.md` ولا يُحلّ بحذف مؤثّرات بصرية.

### الحرّاس

```text
typecheck · build:local · probe مطابق · features ALL PASS
nav-feedback · motion-shell · course-transition   الثلاثة PASS
بصمة الملفات المحمية بلا تغيير · فرقها ٠ بايت
router.refresh() متحقَّقٌ منه سلوكيًّا بعد المسّ بـ PageTransition
ترطيب المنطقة العامّة سليم تحت popLayout
```

> `nav-feedback-regression` صار ثمانية ثوابت، و**كلٌّ منها مُثبَتٌ
> أنه يسقط**: كُسر كل ثابتٍ عمدًا وأُعيد.

### ملاحظةٌ لمن يقيس بعدي

تبويبٌ واحد يمرّ بعشرات التنقّلات المتتابعة يراكم مقاطعَ في ذاكرة
موجّه Next، فيُظهر `main#main` مرّتين (إحداهما بارتفاع صفر). يزول
بإعادة تحميل. **ليس عطلًا** — لكنه يُربك القياس، فأعد التحميل قبل
كل جولة.

## ✅ الجولة الثانية — السلاسة والتنقّل داخل الصفحة (مُنجَزة)



الطلب: «رفع مستوى السلاسة والتحرك (In-Page Navigation & Animations)
إلى أعلى مستوى احترافي». أُجري تدقيقٌ كامل بمنهج `improve-animations`
ثم نُفِّذت النتائج. أربعة إيداعات:

```text
4e69b05  feat(motion): اجعل كل رابطٍ في المنصّة يعترف بالنقرة
ec32012  style(motion): امنح لوحَ الدرس دخولًا بدل أن يُستبدل فجأةً
0e19c4a  fix(motion): علّق تصفير شريط التقدّم على انتهاء التلاشي
c2f63e1  test(motion): حارسٌ ثابت لاعتراف الروابط بالنقرة
```

### ما وجده التدقيق — والنتيجة المهمّة أنّ الأساس كان سليمًا

لا `transition: all`، ولا `ease-in` على عنصر واجهة، ولا `scale(0)`،
ولا حركةٍ بلا `prefers-reduced-motion`. جولات سابقة عالجت ذلك كلّه.
**الفجوة الحقيقية كانت في الفئة الثامنة: ما لا يتحرّك وكان يجب.**

**العلّة الأولى**: نحو أربعين رابطًا لا يعترف بالنقرة. `useLinkStatus`
كان مستعملًا في حبّة `AreaSwitch` **وحدها**؛ أمّا الشريط الجانبي
وتبويبات المقرر والإدارة وبطاقات الكتالوج وصفوف القوائم فلا شيء
يتغيّر فيها بين النقرة ووصول الصفحة — ‏١٠٠٠–١٤٥٠ms مقيسة في
`PORT-HANDOFF.md`. وذلك يُقرأ «الزرّ لا يعمل».

### ما بُني

| الطبقة | الملفّ | الفكرة |
|---|---|---|
| المُعلِن | `components/motion/LinkPending.tsx` | يُركَّب داخل `Link` فيضع `data-pending` على الوسم ويُغذّي العدّاد |
| الغلاف | `components/ui/NavLink.tsx` | بديلٌ مطابق لـ`next/link` يُستورد باسم `Link` — سطرٌ واحد لكل ملفّ |
| العدّاد | `lib/nav-progress.ts` | مخزنٌ خارجيّ؛ عدّادٌ لا راية لأن تنقّلتين قد تتداخلا |
| الشريط | `components/motion/NavProgress.tsx` | خيطٌ ٢px في الجذر، `scaleX` لا `width` |
| المظهر | `globals.css` ‹`a[data-pending]`› | قاعدةٌ واحدة تُلبس كل رابطٍ اللسان نفسه |

ومعها **حالةٌ متفائلة** في الأشرطة الثلاثة: المؤشّر ينطلق مع النقرة
لا بعد وصول الصفحة، و`aria-current` يبقى على المسار الحقيقي.
وصار خطّ الشريط الجانبي وتبويبات الإدارة ينزلقان بـ`layoutId` كما
تنزلق تبويبات المقرر — ثلاثة أشرطة كانت تقول الشيء نفسه بثلاثة ألسنة.

### ⚠ ثلاثة فخاخ في هذا الملفّ بالذات

1. **`NavProgress` في الجذر وحده.** نسختان تعنيان عدّادين يتنازعان
   الخيط. والحارس يمنع تركيبه في تخطيطَي المنطقتين.
2. **`SidebarContent` يُصيَّر مرّتين** — المثبّت واللوحة المنسحبة —
   والمثبّت باقٍ في الشجرة وإن خفي تحت ١٠٦٠px. فلكلٍّ `scope` يفصل
   معرّف الانزلاق، وإلّا قفز الخطّ بين نسختين.
3. **لا تُضِف `loading.tsx` إلى `(public)`.** الحدّ الجديد تحت
   `popLayout` يقتل الترطيب صامتًا (موثّق في CLAUDE.md). والشريط
   العلويّ يغطّي انتظار المنطقة العامّة بلا حاجةٍ إليه — وهو ما
   أغلق الثغرة من غير أن يفتح تلك.

### ما أُصلح في الطريق — كشفه الفحص لا الطلب

- **`origin-[inline-start]` قيمةٌ غير صالحة** لـ`transform-origin`،
  يرفضها المتصفّح صامتًا ويعود إلى المركز. مقيسٌ `50px 5px`. كانت
  شرائح قوّة كلمة المرور تنمو من منتصفها لا من بداية السطر.
- **شريطا الرفع** كانا يحرّكان `width` ستّين مرّة في الثانية طوال
  رفعٍ قد يدوم دقائق. صارا `scaleX`.
- **الزرّ كان يتّسع ٢٤px** لحظة الضغط لأن الدوّارة تُدرَج قبل النصّ.
- **زرّ التأكيد** كان يقفز عرضًا بين «إلغاء الطلب» و«متأكد؟».
- **`transition-[height]`** على علامات الدروس وارتفاعها لا يتبدّل.
- **القفز إلى المرساة** صار ناعمًا — و`MessageThread` ثُبِّت على
  `behavior: "instant"` صراحةً، وإلّا انزلقت المحادثة من أعلاها مع
  كل رسالة.

### القياس

**دورة الشريط، بخنق معالجٍ ٢٠× ليتجاوز الانتظارُ عتبة التسلّح:**

```text
t=348ms   data-pending يظهر على الرابط
t=524ms   الشريط يتسلّح — scaleX 0.079 · opacity 0.66
t=1115ms  يزحف إلى 0.652 متباطئًا بمنحنى ease-out وحده
t=1823ms  المسار يتبدّل، والزحف مستمرّ عند 0.746
t=2459ms  الانتظار ينتهي → قفزةٌ إلى scaleX 1
t=2803ms  scaleX 1 · opacity 0.03  ← يبقى بعرضه حتى ينطفئ
t=2935ms  scaleX 0 · opacity 0     ← ثم يُصفَّر بلا زمن
```

**وبلا خنق:** التنقّلة تكتمل في ‏١٧–٤٢ms والشريط **لا يظهر أصلًا** —
وهو المطلوب. التسبيق يجعل أكثر التنقّلات فوريةً بلا أي طلبِ شبكة.

**عند `prefers-reduced-motion: reduce`،** مقيسًا لا مفترضًا: كتلة
`@media` تُسقط `transform` من قائمة الانتقال وتفرض ‏٠٫١٥s. فالشريط
لا يزحف — يظهر عند ٧٨٪ ثم يقفز إلى المئة ويتلاشى. والخبر يبقى كاملًا.

**والمؤشّر المتفائل:** ينتقل عند ‏٤٠٦ms والمسار ما زال القديم، ثم
يستقرّ بنابضه ‏٣٣٣ ← ٣٤١ ← ٣٤٤ ← ٣٤٥ — حركةٌ لا قفزة.

**والرجوع للخلف والأمام**: المؤشّر و`aria-current` يتبعان المسار
الحقيقي في الاتجاهين، ولا سمة `data-pending` عالقة.

### الحرّاس

```text
typecheck                      PASS
build:local                    PASS
probe.mts                      مطابقٌ للأساس سطرًا بسطر
features.mts                   ALL FEATURES PASS
motion-shell-regression        PASS
course-transition-regression   PASS
nav-feedback-regression        PASS ‹جديد›
logic fingerprint              12c9002331f8242b4d75739213abd743fdafb582bead3f1d2208ec576bb71a4f
protected diff                 ٠ بايت
ستّ شاشات × ثلاثة أعراض        بلا تمرير أفقي · مؤشّرٌ واحد · شريطٌ واحد مخفيّ · بلا سمةٍ عالقة
ترطيب المنطقة العامّة           سليم تحت popLayout (محطّات الكليات تستجيب للنقر)
طرفية المتصفّح                  بلا أخطاء ولا تحذيرات في كل الفحوص
```

> **`nav-feedback-regression.mts` مُثبَتٌ أنه يسقط.** كُسر كل ثابتٍ
> عمدًا وأُعيد؛ الأربعة المفحوصة تسقط برسالتها الخاصّة ثم تمرّ بعد
> الاستعادة. حارسٌ لا يسقط أسوأ من غيابه — وهو ما يحذّر منه
> `PORT-HANDOFF.md` صراحةً عن `features.mts:141`.

### ما بقي مفتوحًا عمدًا

- **خروج الصفوف عند الحذف.** حذف إعلانٍ أو درسٍ يُخفي الصفّ آنيًّا.
  إصلاحه يعني تأخير `router.refresh()` بعد فعلٍ هدّام، ومكسبه
  يخصّ الإدارة وحدها. **لم أفعله، وأراه مقايضةً خاسرة** — يُحسم
  بقرارك لا بصمتٍ منّي.
- **زمن الرفرفة على التصويب** ‏٢٠٠ms في تبويبات المقرر والإدارة
  مقابل ‏١٥٠ms في الشريط الجانبي. فرقٌ لا يُلاحَظ، ولم أوحّده لئلّا
  أحرّك ملفّاتٍ بلا مكسب مقيس.
- **حبّة `AreaSwitch` لا تنزلق** كما تنزلق الأشرطة الثلاثة. تركتُها:
  التخطيطان قد يتعايشان لحظةً أثناء العبور بين المنطقتين، فيتنازع
  مبدّلان المعرّفَ نفسه — وهو الفخّ الذي وقعت فيه تبويبات المقرر.
  المكسب حبّتان متجاورتان؛ لا يستحقّ المخاطرة.

## ✅ الجولة الأولى — دوائر الكليات (مُنجَزة ومُثبَّتة)

> «الخلفية في كلية تقنية المعلومات اجعلها دوائر منطقية على مستويات متعددة
> وشكل افضل ومثل الشي مع كلية الهندسة حسن الشكل»

الإيداع: `52de593` — `style(catalog): أعِد رسم مشهدَي التقنية والهندسة`.
ملفّان فقط: `src/components/public/FacultyStations.tsx` و`src/app/globals.css`.

### السبب الجذريّ الذي لم يكن في الطلب — اقرأه قبل أي عمل على `.faculty-scene`

**المشهد كان مقصوصًا، لا رديئًا.** `.faculty-scene` بارتفاع ٣٦٠px خلف
عمود المحتوى، لكن **بطاقات المقررات معتمة وتغطّي أكثره**. والقياس
بـ`getBoundingClientRect` لا بالنظر — ويتغيّر بعرض النافذة لأن مقياس
الـSVG تابعٌ لعرض العمود:

| عرض النافذة | `xMidYMid` ‹سابقًا› | `xMidYMin` ‹الآن للمشهدين› |
|---|---|---|
| ١٢٨٠px | `y = 0..71` | `y = 0..146` |
| ١٤٤٠px | `y = 0..74` | `y = 0..126` |

التوسيط يهدر نصف المساحة: يدفع الرسم لأسفل بمقدار `(h − 300·k)/2`
فيقع تحت البطاقات. والمحاذاة لأعلى تستردّها. والتكوين الجديد محصورٌ
في `y = 0..130`.

وبوّابات مشهد التقنية المعتمد كانت عند `y = 96..164`، أي **خلف
البطاقات كلّها**؛ فلم يكن يظهر منه إلا خطّان أفقيّان شاردان. ولذلك
بدت الخلفية خاوية.

> **⚠ مشهدا «الآداب» و«العلوم» ما زالا على `xMidYMid` ولهما القصّ
> نفسه.** لم يُمسّا لأن الطلب لم يشملهما. المقيس عند ١٤٤٠px: الآداب
> يرسم `y = 23..244` والظاهر منه حتى `74` وحدها؛ والعلوم أوسع منه
> بمداراته المائلة. أي أن أكثر من ثلثَي كلٍّ منهما تحت البطاقات.
> **هذا مرشَّحٌ واضحٌ للجولة القادمة.**

### ما صار عليه المشهدان

- **التقنية**: دائرة منطقية بأربعة مستويات — أربعة مداخل ← بوّابتا
  AND ← بوّابة OR ← عاكس NOT يتفرّع إلى ثلاثة مخارج. تُرسم **ثلاث
  مرّات** على ثلاثة مستويات عمق (مقاس ١٫٠ / ٠٫٣٦ / ٠٫٢٦ وعتامة
  ١٫٠ / ٠٫٤٠ / ٠٫٢٨) متراكبة. الشكل مُولَّد من `andGate`/`orGate`.
- **الهندسة**: جمالون ورِن بتسع عيون — قطريّ متعرّج واحد متّصل،
  مفصلٌ ثابت وحاملٌ متدحرج، سطحٌ يمتدّ خارج الجمالون، وثلاثة أحمال
  متناظرة حول المنتصف. العدد **فرديّ عمدًا** ليقع الحمل الأوسط في
  وسط البحر حيث أقصى انثناء، وهو نفسه مركز `faculty-flex`.

### قيود التُزِمت (لا تنقضها)

- الحركة `transform`/`opacity` وحدها — لا `stroke-dashoffset` متحرّك.
- الانزياح **داخل** المجموعة المُصغَّرة لا خارجها، فالبعيد يتحرّك أقلّ.
- لا لون جديد. `--color-spark` بقي على `.faculty-pulse` وحدها بحكم
  صنفها القائم؛ خيوط الإشارة `accent-bright`.
- إضافة CSS واحدة: `.faculty-flex` + `@keyframes faculty-flex`.

### الحرّاس بعد الإيداع

```text
typecheck          PASS
build:local        PASS
probe.mts          مطابق للأساس
features.mts       ALL FEATURES PASS
logic fingerprint  12c9002331f8242b4d75739213abd743fdafb582bead3f1d2208ec576bb71a4f
protected diff     ٠ بايت
browser console    بلا أخطاء ولا تحذيرات (متصفّح نظيف، تنقّل بين المحطّات الأربع)
معاينة             ١٤٤٠ · ٧٦٨ · ٣٩٠ — المشهدان كاملان فوق البطاقات في الثلاثة
```

> **⚠ البصمة تغيّرت عن `26084c8f…` المسجَّلة في `PORT-HANDOFF.md`.**
> السبب إيداعات لوحة الإدارة الثلاثة (`6529d6f`, `49eede6`, `2639617`)
> التي مسّت `src/lib/data/*` عمدًا — لا هذه الجولة. القيمة أعلاه هي
> خط الأساس الجديد، وقد بقيت **بلا تغيير** قبل هذه الجولة وبعدها.

## أدوات بُنيت في هذه الجلسة وتستحقّ البقاء

**لقطات الشاشة عبر CDP** — لوحة المعاينة في الوكيل لم تكن معروضة، فلم
تعمل `computer{screenshot}` إطلاقًا. البديل الذي عمل: تشغيل Chrome
بـ`--headless=new --remote-debugging-port` وقيادته من Node عبر
WebSocket المدمج. يسمح بالنقر ثمّ اللقطة — وهو ما تحتاجه محطّات
الكليات لأن الافتراضية دائمًا «الآداب».

السكربتان في `%TEMP%`: `shot2.mjs` (لقطة مقصوصة على `.faculty-scene`
مع `STEPS='["تقنية"]'`) و`console.mjs` (أخطاء الطرفية في متصفّح نظيف).
**انسخهما إلى `scripts/` إن احتجتهما ثانيةً** — ملفّا `%TEMP%` يزولان.

## فخّان أُمسِكا في هذه الجلسة

1. **خادم `next start` قديم كان يعمل على ٣١٠٠ من جلسة سابقة.** بعد
   البناء صار يقدّم HTML يشير إلى قطعٍ حُذفت. أوقِفه **قبل** كل
   معاينة، ثم أعِد تشغيله — لا تكتفِ بإعادة البناء.
2. **`el.click()` وحدها لا تُشغّل معالج React هنا.** أرسِل تسلسل
   `pointerdown/mousedown/pointerup/mouseup/click` أو
   `new MouseEvent('click',{bubbles:true})` مباشرةً.

## قيود ثابتة للمشروع (لا تُنسى)

- ملفات محمية بلا موافقة صريحة: `src/lib/data/*`, `src/app/api/**/*`,
  `src/auth*`, `src/middleware.ts`, `src/server/*`, `prisma/*`.
- `markOrderPaid`/`refundOrder` — كاتب وحيد، لا مستدعٍ ثانٍ أبدًا.
- لا عمليات git هدّامة بلا تعليمات صريحة.
- لا push/merge للـ remote بلا تعليمات صريحة. **`master` متصل
  بـ Netlify ويُنشر تلقائيًا عند كل push.**
- لا `next dev` و`next start` معًا (يشتركان `.next` → `ChunkLoadError`).
- `src/app/(app)/layout.tsx` يظهر مُعدَّلًا في `git status` منذ ما قبل
  هذه الجلسة، و`git diff` له **فارغ**: فرقُ نهايات أسطر لا أكثر. اتركه.

</details>
