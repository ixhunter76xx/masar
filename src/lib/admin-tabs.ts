/**
 * أقسام المنطقة الإدارية — تعيش داخل "الإعدادات" حفاظًا على قاعدة
 * الستة عناصر في القائمة الجانبية.
 *
 * حُذف تبويب "الفصول الدراسية": مسار لا يعرف الفصل الدراسي، المقرر
 * متاح دائمًا. وحُذف "المقررات" مؤقتًا لأن شاشته كانت مبنية على إسناد
 * مدرب وتسجيل طلاب يدوي — وكلاهما لم يعد قائمًا. **وعاد 2026-08-07**
 * بالشكل الذي وُعد به: إنشاء مقرر وتصنيفه، ثم باقاته وأسعارها.
 *
 * ⚠ كل `segment` هنا **يجب** أن يقابله مجلد `app/(app)/settings/<segment>`.
 * التبويب بلا صفحة يعطي 404، والصفحة بلا تبويب لا يصلها أحد.
 */
export const ADMIN_TABS = [
  /* الطلبات أولًا: هي الشاشة الوحيدة التي تحمل عملًا يوميًا ينتظرك،
     وبقية التبويبات مرجعية تُفتح عند الحاجة. */
  { segment: "orders", label: "الطلبات" },
  { segment: "courses", label: "المقررات" },
  { segment: "users", label: "المستخدمون" },
] as const;

/**
 * مسار أول قسم إداري.
 *
 * تحويل الإدارة إلى `/settings` يشتقّ وجهته من هنا لا من قيمة مكتوبة.
 * القيمة الثابتة هي ما كسر المنصة حين حُذف تبويب "الفصول الدراسية":
 * بقي `redirect("/settings/terms")` يشير إلى صفحة غير موجودة، فكان كل
 * دخول إداري ينتهي بخطأ. الآن حذف تبويب يحرّك الوجهة معه.
 */
export const FIRST_ADMIN_TAB = `/settings/${ADMIN_TABS[0].segment}` as const;
