/**
 * وجهات التنقّل المشتركة.
 *
 * ── لماذا ملف لهذا ───────────────────────────────────────────────────
 * وجهة "ما بعد الدخول" مكتوبة في موضعين على الأقل: `middleware.ts` حين
 * يفتح مسجَّلٌ شاشةَ الدخول، و`login/actions.ts` بعد نجاح المصادقة.
 * كتابتها مرتين تعني أنها ستفترق مرة — وهذا ما حدث فعلًا: صار أحدهما
 * يشير إلى `/learn` وهي صفحة لم تُبنَ بعد.
 *
 * حين تجهز بيئة التعلم على `/learn` يكفي تغيير السطر الواحد هنا.
 * ─────────────────────────────────────────────────────────────────────
 */

/** أول صفحة يراها المستخدم بعد الدخول */
export const AFTER_LOGIN = "/dashboard";

/** شاشات المصادقة — عامة، ويُطرد منها من كان مسجَّلًا */
export const AUTH_ROUTES = ["/login", "/signup", "/forgot-password"] as const;
