/**
 * ثوابت الهوية والمحتوى الثابت للمنصة.
 *
 * كل اسم أو رقم أو نصّ علامة يظهر في الواجهة يُقرأ من هنا.
 *
 * ── الشعار لا يُستبدل بنسخ ملفّ ─────────────────────────────────────
 * أصول الهوية سبعةُ ملفّات تُشتقّ آليًّا من مصدرٍ واحد:
 *
 *   npx tsx --tsconfig tsconfig.script.json scripts/generate-icons.mts assets/logo-source.png
 *
 * وتبديل ملفٍّ بيدٍ يكسر `scripts/brand-regression.mts`. السبب أن
 * ثلاثة من السبعة لا تُرى إلا بعد تثبيت التطبيق على الهاتف، فتبقى
 * قديمةً بلا أن يشعر أحد — وهو ما وقع فعلًا.
 */
export const SITE = {
  name: "مسار",
  nameLatin: "MASAR",
  /**
   * ── نسخة الهوية — كاسرُ التخزين المؤقّت ────────────────────────────
   * تُلحق بكل رابط أصلٍ بصريّ. وبدونها لا يرى أحدٌ الشعار الجديد:
   *
   * ١ · **المتصفّح** يخزّن `logo-masar.png` بعنوانه، فيبقى القديم.
   * ٢ · **واتساب** — قناة البيع الأولى لهذه المنصّة — يخزّن صورة
   *     `og:image` لكل رابطٍ يُشارَك، ولا يعيد جلبها إلا بتغيّر
   *     عنوانها. فرابطُ مقرّرٍ شورك أمس يبقى يعرض الهوية القديمة.
   * ٣ · **التطبيق المثبَّت** يحمل أيقوناته من البيان، فتغيّر العنوان
   *     هو ما يدفع النظام إلى إعادة الجلب.
   *
   * ⚠ تُرفع مع كل تغييرٍ في الشعار — يحرسها `brand-regression`
   *   بمطابقتها ببصمة المصدر في `assets/brand-lock.json`.
   */
  brandVersion: "masar-20260822",
  /**
   * أصل الموقع في الإنتاج — مصدرٌ واحد لكل رابطٍ مطلق.
   *
   * ── لماذا ثابتٌ لا متغيّر بيئة ──────────────────────────────────────
   * لا يُقرأ منه توجيهٌ ولا تحقّقُ جلسة — تلك كلها تشتقّ من ترويسة
   * الطلب (`req.nextUrl` في middleware، و`trustHost` في Auth.js).
   * ما يحتاجه هذا الثابت شيءٌ واحد: **الروابط المطلقة في الوصف**
   * (`metadataBase`, Open Graph, sitemap) — وهي بطبيعتها تشير إلى
   * النطاق الرسميّ لا إلى النطاق الذي صادف أن فُتحت منه الصفحة.
   *
   * ولذلك لا يضرّ أن تُفتح معاينةٌ على نطاقٍ آخر: بطاقة المشاركة
   * تبقى تشير إلى الموقع الحقيقي، وهو الصواب.
   */
  url: "https://masar-bh.com",
  tagline: "منصة مسار لشرح المقررات الجامعية",
  /**
   * سطر قصير تحت الاسم في الشريط الجانبي — `tagline` أطول من أن يتّسع
   * هناك فيُقتطع. كان مكانه نصّ ثابت «للتعليم والتدريب»، وهي لغة مركز
   * تدريب عام تناقض ما تقوله الصفحة العامة عن نفسها.
   */
  shortTagline: "شرح المقررات الجامعية",
  shortDescription:
    "شرح مركّز للمقررات الجامعية، تشتري ما تحتاجه منها وتدرسه في مكان واحد.",
  supportPhone: "33060460",
  copyrightYear: "2026",
} as const;
