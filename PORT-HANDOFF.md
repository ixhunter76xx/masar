# تسليم نقل التصميم — فرع `masar-port`

كُتب 2026-08-14، وحُدّث بعد إيداع النقل والجولة البصرية الجزئية وجولة
الأداء في 2026-08-19. **اقرأه كاملًا قبل لمس أي شيء** — وابدأ بجدول التغطية،
فهو وحده يقول أيّ شاشةٍ عُوينت وأيّها لم تُعاين.

## ⚡ تحديث الأداء — مُودَع ومقاس، لا تخمينًا بصريًّا

جولة 2026-08-19 عالجت بطء البيانات والحركة معًا في ثلاثة إيداعات:

```text
9b572f6  perf: cache public course data
b1d3b70  perf: streamline motion and visual effects
bbcc77e  fix: guard shell data during parallel rendering
```

### ما كان بطيئًا وما تغيّر

1. **الكتالوج وصفحة المقرر كانا يعيدان استعلام Neon لكل طلب.** صار
   الجزء العام وحده في `unstable_cache` لمدة 60 ثانية، مع وسم يُبطَل
   فور كتابة الإدارة. بيانات الجلسة وملكية الطالب **ليستا في المخزن**.
2. **انتقال الصفحة كان متسلسلًا**: خروج 160ms ثم دخول 380ms تحت
   `mode="wait"`. صار الخروج 100ms والدخول 240ms متوازيين تحت
   `popLayout`، مع إبقاء إصلاح `FrozenRouter` و`router.refresh()` كما هو.
3. **سبع حركات غير مركّبة** كانت تغيّر `stroke-dashoffset` و`height`
   و`top` و`box-shadow`. صارت العلامة والسكّة والهالة على `transform`،
   وهالة العقدة على `transform/opacity`، والمسارات الدائمة ثابتة.
4. **لوح الدرس ومشهد الكلية كانا يعملان خارج الإطار.** صارا يتوقفان
   خارج الرؤية وعند إخفاء التبويب، مع بقاء التصفح اليدوي كاملًا.
5. أزيل `blur(24px)` عن طبقة ضوء بحجم 572×752px، وخُفّض تمويه الرأسية
   من 24px إلى 12px مع رفع عتامة السطح؛ النتيجة أصفى لا أفقر.
6. خط أميري نفسه باقٍ في لوح الإعراب، لكنه صار مُحمّلًا في مسار
   الكتالوج لا في جذر كل صفحات المنصة.

### القياس قبل → بعد

الأرقام التالية من **الزمن المرصود** في تتبع Lighthouse لا من نموذج
Lantern وحده؛ الاختبار المتنقل يضيف اختناق شبكة اصطناعيًّا:

| الصفحة | TTFB المرصود | FCP/LCP المرصود | Speed Index المرصود |
|---|---:|---:|---:|
| `/courses` هاتف | 3990ms → **456ms** | 4179ms → **486ms** | 4340ms → **529ms** |
| `/courses/arab110` هاتف | 3254ms → **455ms** | 3367ms → **461ms** | 3400ms → **510ms** |
| `/courses` سطح مكتب | 2990ms → **125ms** | 3143ms → **544ms** | 3237ms → **596ms** |

والقياس المباشر بعد الإحماء، بلا اختناق Lighthouse:

```text
/courses          ~438ms → 8–10ms
/courses/arab110  ~655ms → 11–13ms
```

الحركات غير المركّبة صارت **0** من 6 على هاتف الكتالوج، و**0** من 7
على سطح المكتب، و**0** من 1 في صفحة المقرر. بقي CLS دون 0.006.

> ⚠ درجة Lighthouse المتنقلة المصطنعة للكتالوج تذبذبت 93→75 رغم أن
> الزمن المرصود تحسّن قرابة ثماني مرات؛ نموذج Lantern في تشغيل محلي
> واحد خلط شبكةً مصطنعة بطلب قاعدة بارد. لا تستعمل الدرجة المفردة
> معيار قبول هنا. درجة سطح المكتب ارتفعت 94→99، لكن معايير القبول هي
> الأرقام المرصودة أعلاه، وTTFB المباشر، وعدّ الحركات غير المركّبة.

### الحراس بعد جولة الأداء

```text
typecheck          PASS
build:local        PASS
probe.mts          PASS
features.mts       ALL FEATURES PASS
logic fingerprint  cd98c04023ad3d9c44185872a17cdb303fd1a771e0bda1dc7bb28ac0016821e6
browser console    بلا أخطاء أو تحذيرات
```

### الجولة الثانية — زمن المسارات المحمية وقاعدة البيانات

قياس Chrome الفعلي أظهر أن النقر والحركة ليسا عنق الزجاجة: بلغ INP بين
4–15ms، وCLS بين 0 و0.016، ولم يقدّر المتصفح أي وفرٍ من إزالة الموارد
الحاجبة للرسم. التأخير كان داخل استجابات RSC الخادمية، التي بقي بعضها
مفتوحًا 3.1–4.4 ثوانٍ بانتظار البيانات.

السبب المقاس: الخادم المحلي في البحرين يتصل بقاعدة Neon في
`us-east-2`. فتح اتصال جديد كلّف نحو 1.5 ثانية، وكل جولة بعد إعادة
الاستعمال نحو 211ms. لذلك نُفّذ الآتي من دون تخفيض جودة الحركة أو البلور:

1. صار عميل التطبيق `PrismaNeon` عبر WebSocket بدل `PrismaPg`، مع إبقاء
   `PrismaPg` لسكربتات الإدارة. لم يُستعمل `PrismaNeonHttp` لأنه لا يدعم
   المعاملات التفاعلية التي تعتمد عليها الطلبات والرسائل.
2. صار `DATABASE_URL` المحلي يشير إلى Neon pooler. لا يحتوي `.env`
   المحلي حاليًا `DIRECT_URL`؛ لا يحتاجه تشغيل الموقع، لكن يجب إضافة
   رابط Neon المباشر قبل أي هجرة محلية. الملف متجاهَل من Git، لذلك يجب
   التحقق من المتغيرين يدويًا في أي جهاز جديد.
3. يحتفظ الخادم المحلي بالاتصال الخامل خمس دقائق؛ وتبقى مهلة Netlify
   عشر ثوانٍ حتى لا تتراكم الاتصالات بين النسخ الخادمية.
4. توازت القراءات المستقلة في `requireCourseAccess` و`accessibleLessonIds`
   و`getCourseResume` و`getCourseAssignments`، مع إبقاء بوابات الوصول
   والتصفية النهائية كما هي.

### قياس الجولة الثانية

| القياس | قبل | بعد |
|---|---:|---:|
| `/dashboard` — تشغيل بارد، LCP | 2902ms | **2325ms** |
| `/dashboard` — تشغيل بارد، render delay | 1131ms | **747ms** |
| `/dashboard` — اتصال دافئ، LCP | — | **1165ms** |
| `/dashboard` — اتصال دافئ، TTFB | — | **664ms** |
| انتقال `/profile` المرصود | 3739ms | **1001ms** |
| انتقال `/messages` المرصود | 3781ms | **1450ms** |

أصبحت طلبات prefetch للمقرر والإعلانات قرابة 925–939ms بدل نحو 1370ms.
يبقى زمن 211ms لكل جولة قاعدة حدًّا جغرافيًّا حقيقيًّا؛ للوصول إلى أقل
من 500ms باستمرار لكل شاشة بيانات يجب تشغيل التطبيق قرب `us-east-2` أو
نقل قاعدة البيانات قرب المستخدمين، لا حذف مزيد من المؤثرات البصرية.

```text
typecheck          PASS
build:local        PASS
probe.mts          PASS
features.mts       ALL FEATURES PASS
logic fingerprint  26084c8fd06e1c436363f94421aed4f7949dcb1f51c6a629d376dae2f3907bad
browser console    بلا أخطاء أو تحذيرات
```

## ✅ الحالة الحالية — النقل مُودَع، والجولة البصرية **جزئية** لا مكتملة

حُدّث 2026-08-19. النقل الذي كان يعيش في شجرة العمل صار ثلاثة إيداعات،
وأُجريت جولة بصرية على الشاشات المحمية **غطّت بعضها لا كلّها**. اقرأ
جدول التغطية أدناه قبل أن تفترض أن شاشةً عُوينت.

```
30dbfed  مرّر حقول الإدارة الأربعة على `.input-field`
8b80b6d  عرّب الأرقام حيث بقيت لاتينية
64af1d4  انقل التصميم المعتمد إلى الشاشات
```

### الحراس بعد آخر إيداع

```text
typecheck          PASS
build:local        PASS — بلا تحذير
probe.mts          مطابقة للأساس حرفًا بحرف
features.mts       ALL FEATURES PASS
logic fingerprint  cd98c04023ad3d9c44185872a17cdb303fd1a771e0bda1dc7bb28ac0016821e6
protected diff     فارغ (٠ بايت)
git diff --check   نظيف
```

### ما وجدته الجولة وأُصلح

**١ · الأرقام اللاتينية — أوسع ممّا بدا.** أوضح دليل: صفحة الدرجات
كانت تعرض الرقم نفسه بصورتين في شاشة واحدة — «٤ / ٤٪» في ملخّص المقرر
و«4 / 4» و«100%» في الصفّ تحته. والجذر في `format.ts`:
`Intl.DateTimeFormat("ar")` تُرجع في ICU الحاليّ `numberingSystem:
"latn"`، فكان تعليق `formatDate` يَعِد «٤ أغسطس ٢٠٢٦» والشاشة تقول
«5 أغسطس 2026». صار `ar-u-nu-arab` صراحةً.

**وما لا يُعرَّب، عمدًا:** حقل السعر في `ProductManager` (يمرّ من
`Number()`)، وأرقام الطلبات والهواتف والبُرُد ورموز المقررات (تُنسخ
إلى واتساب وإلى تطبيق المصرف)، ونصّ الإعلانات والرسائل (كتبه إنسان).
ولذلك يُعرَّب `detail` في سجل النشاط عند `kind === "grade"` وحدها.

**٢ · أربعة حقول لم تمرّ على `.input-field` أصلًا.** كلها في شاشات
الإدارة: `OrderReviewCard` ×٢، و`ProductManager`، و`LessonPlanner`.
كانت 10px · 44px · `bg-ink` بتركيزٍ بلا حلقة و`hover:` بلا
`@media (hover:hover)`؛ صارت 12px · 46px · `--sunk` — مقيسةً في
المتصفّح قبل وبعد.

> ⚠ **الدرس المنهجيّ:** عدّ المواضع لا يكشف هذا. `.input-field` يظهر
> في ملفّين فقط لأنه صنفٌ يستهلكه `Field.tsx` وتَرِثُه النماذج الثلاثة
> عشر من غير أن تسمّيه — وهذا **تجريدٌ صحيح**. والانحراف كان في حقولٍ
> لم تدخل المنظومة أصلًا، فلا يظهر في أي إحصاء لها. **قِس الحقل على
> الشاشة، لا عدد مرّات ذكر الصنف.**

### جدول التغطية — ما عُوين فعلًا، وما لم يُعاين

حُدّث بعد جولة 768 في 2026-08-19. القياس بـ`getBoundingClientRect`
و`scrollWidth`، لا بالنظر.

| الشاشة | 1440 | 768 | 390 |
|---|---|---|---|
| `/dashboard` طالب | ✅ | ✅ | ✖ |
| `/learn` طالب | ✅ | ✅ | ✖ |
| `/learn/[id]` المحتوى | ✅ | ✅ | ✖ |
| `/learn/[id]` الإعلانات | ✖ | ✅ | ✖ |
| `/learn/[id]` الدرجات | ✖ | ✅ **كشفت انحرافًا** | ✖ |
| `/learn/[id]` الرسائل | ✖ | ✅ | ✖ |
| `/grades` طالب | ✅ | ✅ | ✅ |
| `/orders` طالب | ✅ | ✅ | ✖ |
| `/profile` طالب | ✅ | ✅ | ✖ |
| `/messages` طالب | ✖ | ✅ | ✖ |
| `/login` زائر | ✅ | ✖ | ✖ |
| `/settings/orders` إدارة | ✅ | ✅ | ✅ |
| `/settings/users` إدارة | ✖ | ✅ | ✖ |
| `/settings/courses` إدارة | ✅ | ✅ | ✖ |
| `/settings/courses/[id]` | ✅ | ✅ | ✖ |
| اللوحة المنسحبة | — | ✖ | ✅ |
| **`/courses` و`/courses/[slug]`** | ✅ | ✖ | ✅ |
| صفحات الاختبار والواجب التفصيلية | ✖ | ✖ | ✖ |

**عرض 768 مغطّى الآن على خمس عشرة شاشة**، وكلها نظيفة:
`scrollWidth === clientWidth`، لا عنصر أعرض من الإطار، ولا صور
مكسورة، والحقول 12px بارتفاع 46–48px والقوائم المصغَّرة 44px.
وكشفت الجولة انحرافًا واحدًا أُصلح في `263bbcf`.

⚠ **وما زالت صفحتا الكتالوج والمقرر العامّتان خارج القياس عند 768** —
وهو موضع شبكة الباقات الثلاثية (760–1059px). أُخّرتا عمدًا: القرار ٣
أدناه يضيف قسمًا إلى صفحة المقرر، فقياسها قبل حسمه قياسٌ مرّتين.

ولم تُفحص بعد: حالات hover وfocus ولوحة المفاتيح، وحالات التحميل
والخطأ، والنصّ الطويل. (الحالة الفارغة عوينت في تبويب الرسائل: «لا
رسائل بعد».)

> ### ⚠ فخّان منهجيّان في الجولة نفسها — لا في المنتج
>
> **١ · `grep -c` يعدّ الأسطر لا المطابقات.** فحصتُ `/settings/users`
> بـ`grep -c 'role-'` على HTML مُصغَّر يضع الصفحة في سطرٍ واحد،
> فعادت `1` واستنتجتُ أن قائمة المستخدمين لا تُعرض — ثم «أكّدتُ» ذلك
> على الإنتاج بالخطأ نفسه. والصحيح أن الشاشة **سليمة**: ستّ قوائم
> `role-<id>` وستّة أسماء، وقياسٌ لاحق في المتصفّح أعطى
> `main = 1061px` بلا حالة تحميل. **استعمل `grep -o | wc -l`.**
>
> **٢ · `features.mts:141` يفحص رمز الحالة وحده** ‹`users.status === 200`›.
> والقاعدة المسجّلة في §١ تقول إن رمز الحالة **لا يميّز** في المنطقة
> المحمية، لأنها ترجع 200 على «غير موجود». الشاشة تعمل اليوم، فالفحص
> لا يُنتج نجاحًا كاذبًا الآن — لكنه **لن يمسك** انكسارها غدًا.
> علامةٌ لا توجد إلا في الصفحة المقصودة هي الإصلاح.
>
> **وتصحيحٌ لتشخيص ثالث:** ظهر في سجل السيرفر
> `TypeError: Cannot read properties of null (reading 'user')`، وسُجّل
> هنا أولًا أنه أثر أداة الفحص. كان ذلك **خاطئًا**: `AppPage` افترض أن
> تحويل التخطيط سبق تصيير الصفحة، بينما App Router قد يصيّرهما بالتوازي.
> أُصلح في `bbcc77e` بحارس جلسة صريح، وأعيد `probe` و`features` بعدها
> بلا استثناء في سجل الخادم.

### ما بقي — وثلاثةٌ منه قرارُ مالك لا شغل تنفيذ

1. **إكمال الجولة**: الكتالوج وصفحة المقرر العامّة عند 768 و390
   (بعد القرار ٣)، وما بقي ✖ في الجدول أعلاه، وحالات hover/focus
   ولوحة المفاتيح.
2. **قرارا المحتوى الأصليان** كما هما: الكلية في فتات الطريق
   (`getPublicCourse` داخل البصمة المجمَّدة)، وحقلُ مثالٍ محلول لملصق
   المشغّل. لا تُكتب جملة إعراب ثابتة تكذب على المقررات الأخرى.
3. **⟨جديد⟩ `course.description` لم يعد يُعرض في واجهة المتجر.** حُذف
   قسم «عن المقرر»، وهذا **مطابقٌ للمصدر** — صفحة المقرر في المعاينة
   لا تحوي قسمًا كهذا، ونموذجها لا يحمل إلا `sum` على البطاقة. لكن
   الحقل مملوء فعلًا في قاعدة البيانات:

   ```text
   ARAB110  summary     → يُعرض على بطاقة الكتالوج
            description → «يغطّي المقرر الاستفهام والصرف والنحو
                           والبلاغة…» — يُقرأ من قاعدة البيانات
                           ولا يُعرض في المتجر
   ```

   وهو **يظهر في قاعة الدرس** (`CourseHeaderCard`)، أي أن الخسارة
   تقع على **المشتري قبل الشراء** وحده — وهو بالضبط من كُتب النصّ له.
   القرار: يُعاد قسمٌ للمتجر (انحرافٌ عن المصدر)، أم يبقى الحقل
   لقاعة الدرس وحدها؟ **لم أُقرّر، ولم أستبدله بنصّ ثابت.**

4. **⟨جديد⟩ ٤٤ موضعًا تُثبِّت `rounded-[10px]` في ٢٦ ملفًّا** مقابل
   ١٥ موضعًا تستعمل `rounded-field`. الرمز نفسه يقول «الحقول
   **والأزرار**» عند 12px، فهذه أزرارٌ تخالف الرمز. لم أكنسها: تمسّ
   كل شاشة في المنتج، وتحتاج جولةً بصرية لا تملكها هذه الجلسة —
   وCLAUDE.md يقول صراحةً إن المترجم لا يمسك هذا الصنف من الانحراف.
   ذيلُ الانحراف رقم ٥، ويُحسم معه.

5. **⟨جديد⟩ «مساء الخير» ثابتةٌ في لوحة التحكم** — منقولةٌ حرفيًّا من
   السطر ١٤٠١ في المصدر. المعاينة لقطةُ لحظة، والتطبيق يُعرض طوال
   اليوم؛ فهي تقول «مساء» في التاسعة صباحًا. اشتقاقها من الساعة
   سلوكٌ لا تنسيق، فتُركت للمالك.

> **حدّ منهجيّ في الجولة، اعرفه قبل أن تثق بلقطاتها:** الرمز المسكوك
> بـ`mint-session.mts` لا يحمل `name`، بينما تسجيل الدخول الحقيقي
> يحمله (`auth.config.ts` يضع `token.name`). فتحية اللوحة ظهرت
> «أهلًا، بك» لا «أهلًا، ‹الاسم›». **هذا أثرُ أداة الفحص لا عيبٌ في
> المنتج** — ولم يُعايَن مسار الاسم الطويل في العنوان أصلًا.


---

## ⚠ الخطأ المنهجيّ الذي سبّب كل ما تلاه

المعاينة المعتمدة **ليست على هذا الفرع**: تعيش في `design/vision/` على
فرع `masar-vision`. فنُقلت الأجزاء الأولى **من الذاكرة لا من الملف**،
وأنتج ذلك ادّعاءات خاطئة في تقريرين متتاليين.

**القاعدة الملزِمة لكل من يكمل:** قبل نقل أي عنصر، افتح المصدر:

```bash
git show masar-vision:design/vision/index.html > /tmp/vision.html
```

ثم اقرأ العنصر بسطره، ولا تعتمد على وصفٍ في هذا الملف ولا في رسالة
سابقة. هذا الملف فهرسٌ للمصدر، لا بديلٌ عنه.

---

## ١ · آلية الضبط — لا تُمسّ

ثلاث أدوات تحرس النقل، وكلها تعمل. **شغّلها بعد كل جزء:**

```bash
npm run build:local
npx tsx --tsconfig tsconfig.script.json scripts/probe.mts     # يجب أن يطابق الأساس
npx tsx --tsconfig tsconfig.script.json scripts/features.mts  # ALL FEATURES PASS
```

**البصمة المنطقية** — ٣٨ ملفًّا لا يجوز أن يمسّها نقلٌ بصريّ:

```bash
LC_ALL=C git hash-object $(git ls-files 'src/lib/data/*' 'src/app/api/**/*' 'src/auth*' \
  'src/middleware.ts' 'src/server/*' 'prisma/*' | LC_ALL=C sort) | sha256sum
# يجب أن تبقى: 26084c8fd06e1c436363f94421aed4f7949dcb1f51c6a629d376dae2f3907bad
```

> تغيّرت البصمة من `cd98c040…` إلى `26084c8f…` في جولة أداء المسارات
> المحمية بتاريخ 2026-08-19. التغيير مقصود ومحصور في توازي قراءات مستقلة
> وعميل اتصال Neon؛ وقد مرّ `probe.mts` و`features.mts` كاملين. البصمة
> الجديدة هي خط الأساس لأي نقل بصري تالٍ.

> ⚠ **القيمة المسجَّلة سابقًا (`9a3392af…`) كانت خاطئة، وصُحّحت 2026-08-14.**
> لم تُنتِجها الشجرة عند **أيّ** إيداع على هذا الفرع — لا عند `c7c850d`، ولا
> عند `f95d0eb` نفسه الذي كُتب فيه هذا الملف. فمن نسخها كان يقارن برقمٍ
> لا يطابق شيئًا: الحارس كان يرفع إنذارًا كاذبًا في كل تشغيل، وهو أسوأ من
> غياب الحارس لأنه يُدرَّب على تجاهله.
>
> **و`LC_ALL=C` جزءٌ من الوصفة لا زينة:** ترتيب `en_US.UTF-8` يُهمل علامات
> الترقيم في المقارنة، فيزحزح مسارات `[courseId]` ويعطي بصمةً أخرى
> (`91d9033a…`) لشجرةٍ **متطابقة**. بلا تثبيت اللغة، الحارس يتّهم البريء.

**والضمان الأقوى لا يحتاج بصمةً أصلًا** — أن الشفرة لم تُلمس يثبته git مباشرةً،
بلا حساسيةٍ للغة ولا لنهايات الأسطر:

```bash
git log --oneline masar-design-2..masar-port -- 'src/lib/data/*' 'src/app/api/**/*' \
  'src/auth*' 'src/middleware.ts' 'src/server/*' 'prisma/*'
# بعد الجولة الثانية يجب ألا يظهر تغيير غير إيداع أداء المسارات المحمية
```

⚠ التجميد مرجعه `masar-design-2` لا `master`: مرحلة التصميم (PR #1) غيّرت
`src/lib/data/` عمدًا (`getCourseResume`، وإرخاء مرشِّح `getCourseMaterials`).
فمقارنةُ البصمة بـ`master` تُظهر فرقًا صحيحًا ومقصودًا.

⚠ **المِجَسّ نفسه أخطأ مرّة** وأبلغ عن تسريب صلاحيات لا وجود له: كان
يطابق «الطلبات» فالتقط **«طلباتي» في شريط الطالب**. العلامة يجب أن تكون
نصًّا لا يوجد إلا في الصفحة المقصودة — ورمز الحالة **لا يميّز** المسموح
من الممنوع، لأن المنطقة المحمية ترجع 200 على «غير موجود».

---

## ٢ · ما نُقل فعلًا ومتحقَّق منه

| العنصر | المصدر | الوجهة في التطبيق |
|---|---|---|
| اللوحة والرموز | `:root` | `src/app/globals.css` |
| اللوح (البطل) | `.board*` `.parse` `.pw*` | `components/public/LessonBoard.tsx` + `lib/board-examples.ts` |
| مبدّل المنطقتين | `.areaswitch` 314 | `components/shell/AreaSwitch.tsx` |
| حلقة التركيز · `.numeric`/`.code` | 106 | `globals.css` |
| الأرقام العربية وتمييز العدد | 1613-1615 | `lib/numerals.ts` + `components/ui/Num.tsx` |
| عمود الدروس | `.course-spine` 1692 | `components/public/CourseCard.tsx` |
| ملصق المشغّل + الزرّ المُسمّى | `.player-poster` `.player-cta` | `(public)/courses/[slug]/page.tsx` |
| فتات الطريق | `.crumbs` 1106 | نفس الملف |
| العنوان والفقرة | 1058 | `(public)/courses/page.tsx` |
| **الحقل ومرجعه الواحد** | `.input` 628-643 | `globals.css` ‹`.input-field`› |
| **التسمية · المساعدة · الخطأ** | `.label` `.help` `.help.danger` 627 · 644-650 | `components/ui/Field.tsx` |
| **منطقة النصّ · القائمة** | 1267-1277 | `Textarea` · `Select` في الملفّ نفسه |
| **مربّع الاختيار** | `.check` 662-675 | نفس الملفّ |
| **تنبيه النموذج** | `.authalert` 681 | `components/ui/FormAlert.tsx` |
| **بطاقة المصادقة** | `.authcard` `.authhead` 679-680 | `(auth)/layout.tsx` · `login` · `signup` · `forgot-password` |
| **زرّ كشف كلمة المرور** | `.pwwrap` `.pwbtn` 653-658 | `components/ui/PasswordField.tsx` |

---

## ٣ · ما **لم** يُنقل — بأرقام أسطر المصدر

### أ · ~~المصادقة والحقول~~ — **تمّت 2026-08-14**

نُقلت حالات الحقل السبع كلها، وتُشتقّ منها النماذج الثلاثة عشر فعلًا لا
اسمًا. تفصيلها في §٢ أعلاه. وما يستحقّ البقاء هنا هو **الانحراف الذي
كشفه النقل**، لأنه يقيس صحّة الترتيب المقترح في §٧:

الشكل نفسه كان مكتوبًا بخطّ اليد **تسع مرّات**: ستّ مناطق نصّ خمسٌ منها
متطابقة حرفًا بحرف، و`admin/Select.tsx` نسخةٌ من أنماط `Input` تفرّعت
عنها، وقائمتان مصغَّرتان. وتنبيه النموذج كان بثلاث صور مختلفة — شريط
محدود في `LoginForm`، ونسخته في `SignupForm`، و**نصٌّ أحمر عارٍ** بلا
حدٍّ ولا رمز في `ChangePasswordForm`. كلٌّ منها يُقرأ صحيحًا وحده،
ومجتمعةً تقول إن الخطأ يبدو مختلفًا حسب الشاشة التي وقع فيها.

**ولم يكن أيٌّ من ذلك مرئيًّا في الشاشات** — ولذلك لم يكن ليُكتشف بجولة
تصفّح. اليوم: صنفٌ واحد `.input-field`، ومكوّنٌ واحد `FormAlert`.

⚠ **القائمتان المصغَّرتان** (`UserRoleSelect` · `CoursePresenterSelect`)
تتجاوزان **المقاس وحده** ‹`w-auto` · `min-h-touch` · `text-[11px]`›
وترثان اللون والحدّ وحلقة التركيز. هما ضابطان داخل صفّ جدول لا حقلا
نموذج، والمعرض في المصدر لا يمثّل هذا المقاس أصلًا. لا تُوسِّعهما إلى
`46px` طلبًا للتماثل — سيكسر جدولَي المستخدمين والمقررات.

### ب · الدرجات والبيانات الكثيفة
| صنف | سطر | الوجهة |
|---|---|---|
| `.tablewrap` `.gt` | 689-691 | `components/grades/GradebookTable.tsx` |
| `.ring` | 701 | `components/grades/ScoreRing.tsx` |
| `.empty` `.empty-mark` | 712-713 | `components/ui/EmptyState.tsx` |
| `.sk` `.sk-row` | 720-728 | `components/ui/Skeleton.tsx` |

⚠ لمعة الهيكل تسير **يمينًا→يسارًا** في RTL (`@keyframes skwaveRtl`).
لمعةٌ ضدّ اتجاه القراءة تُقرأ خللًا في الرسم.

### ج · صفحة المقرر — الباقات
| صنف | سطر | ملاحظة |
|---|---|---|
| `.tiers` | 913 | **ثلاث جنبًا إلى جنب فوق ٧٦٠px** — الصفحة اليوم عمود جانبيّ |
| `.tier` `.tier-badge` `.tier-price` | 921-929 | `minmax(0,1fr)` لا `1fr` — الحدّ الأدنى الضمنيّ يدفع الشبكة خارج الصفحة |
| `.bundle` `.bundle-head` `.bundle-rail` | 896-907 | تجميع الدروس تحت قوس الباقة |
| `.strike` `.tick` | 932-935 | السعر القديم مشطوبًا بـ`--subtle` لا `--faint` (٣٫٠٤ دون AA) |

### د · لوحة التحكم وبيئة الدراسة
| صنف | سطر |
|---|---|
| `.resume` `.meter` | 964 · 955 |
| `.mini` `.dash-grid` | 975 · 969 |
| `.feed` `.feed-row` | 979-980 |
| `.study` `.tabs` `.tab` | 938 · 945 · 948 |
| `.stop` `.stop-body` `.rail` `.node*` | 371 · 372 · 351 · 355 |

### هـ · الهيكل — نُقل المبدّل فقط، وبقيت تفاصيله
| صنف | سطر | ملاحظة |
|---|---|---|
| `.navlist` `.navlink` | 552-555 | الخطّ الرأسي عند الحدّ الفاصل، وليس عند حافّة البطاقة |
| `.countbadge` | 574 | ⚠ عيبٌ وقعتُ فيه: `span` مجرّدة تطابق العدّاد فيرث `flex:1` ويتمدّد إلى ٨٨px |
| `.userchip` `.side-id` | 580 · 545 | |
| `.drawer*` | 605 | اللوحة المنسحبة — `MobileNav` قائم ولم يُقارن |
| `.skip` | 186 | رابط التخطّي — موجود في التطبيق بشكل مختلف |
| `.scene-art` `.glyphs` `.station*` | 760 · 795 · 734 | `FacultyStations` من PR #1، لم يُقارن بالمصدر |

---

## ٤ · انحرافات مُعلَنة تنتظر حكم صاحب المنتج

| # | الانحراف | السبب | البديل إن رُفض |
|---|---|---|---|
| ١ | **«إنشاء حساب» باقٍ** في الرأسية العامّة | المعاينة تعرض زرّ «دخول» وحده، وحذف مسار التسجيل من رأسية سوقٍ قرارُ منتج | احذفه وابقِ «دخول» |
| ٢ | **لا رأسية عامّة لمستخدم مسجَّل** | المعاينة لا تُمثّل هذه الحالة أصلًا؛ الحالة الآن فارغة والمبدّل هو الباب | صمّم الحالة |
| ٣ | **ملصق المشغّل يعرض عنوان الدرس** لا إعرابًا | لا حقل في المخطط يحمل «مثالًا محلولًا»؛ نصٌّ ثابت يكذب على مقرر غير عربي | أضف حقلًا تحريريًّا، أو اقصر الإعراب على مقررات الآداب |
| ٤ | **الكلية محذوفة من فتات الطريق** | `getPublicCourse` لا تُرجعها، وملفّها **داخل البصمة المجمَّدة** | ائذن بفكّ التجميد لإضافة `faculty` إلى `select` — تغييرٌ قرائيّ بحت |
| ٥ | **نصفا القطر يخالفان المصدر** | `--radius-field` ‏**١٠px** والمصدر `--r-field` ‏**١٢px**؛ `--radius-card` ‏**١٤px** والمصدر `--r-card` ‏**١٨px**. لم أغيّرهما: رمزان عامّان يمسّان كل حقل وزرّ وبطاقة في المنتج، وذلك أوسع من «الحقول» | سطران في `globals.css` — قُل «نعم» وتُطبَّق دفعةً واحدة |
| ٦ | **نبرة النجاح في `FormAlert` زيادة على المصدر** | المعاينة تعرّف `.authalert` بنبرة الخطأ وحدها، والمنتج يحتاج شريط نجاح («تم تغيير كلمة المرور») | اشتُقّت بالصيغة نفسها ‹حدّ ٤٠٪ · خلفية ١٠٪› بـ`--color-success` و«✓». احذفها إن رُفضت |

⚠ **وتصحيحان لهذا الملفّ نفسه، وُجدا 2026-08-14:**

1. **قيمة البصمة كانت خاطئة** — صُحّحت في §١، وتفصيلها هناك.
2. **«`.pwwrap` `.pwbtn` — لا زرّ كشف اليوم» كان خبرًا خاطئًا.**
   `PasswordField.tsx` يحمل زرّ الكشف منذ ما قبل النقل، بـ`aria-pressed`
   و`aria-controls` وتبديل `type` وحده حفاظًا على التركيز — بل ويحمل
   **مؤشّر قوة لا وجود له في المصدر أصلًا**. لم يُنقل إلا مقاسه
   ‹٤٠px مركَّزًا› ونصفُ قطره رمزًا لا رقمًا.

   وكلا الخطأين من الصنف الذي يحذّر منه §٠: وصفٌ كُتب من الذاكرة. **من
   يكمل: تحقّق من كل سطرٍ هنا بفتح الملفّ المذكور، لا بقراءة وصفه.**

---

## ٥ · حالة الفرع

```
masar-port  ←  masar-design-2 (PR #1)  ←  master
```

**`masar-port` يحتوي PR #1 ويغنيه عنه** — فالدمج واحد لا اثنان.

⚠ **لم يُدفَع إلى الخادم البعيد.** الدفع يحتاج بيانات اعتماد المالك.

> ### ~~⚠ عمل المرحلة الأولى في شجرة العمل، غير مودَع~~ — **أُودِع 2026-08-19**
>
> كان هذا التحذير صحيحًا حتى 2026-08-19، وهو الآن **منقوضٌ بالكامل**:
> النقل كلّه مُودَع في `64af1d4`، ومعه `FormAlert.tsx` و`OfferMap.tsx`
> اللذان كانا غير متتبَّعين. فما يصفه §٢ و§٣.أ صار يصف **آخر إيداع**،
> لا شجرة العمل.
>
> ```bash
> git log --oneline -3   # bbcc77e · b1d3b70 · 9b572f6
> git status --short      # نظيفة — لا عملَ معلّقًا
> ```
>
> يبقى من الفقرة الأصلية درسٌ واحد يستحقّ الحفظ: **شجرةٌ نظيفة لم تكن
> يومًا دليلَ سلامة على هذا الفرع.** من قرأ التحذير القديم بعد الإيداع
> كان سيستنتج أن العمل ضاع. لذلك يُصحَّح التوثيق مع الحدث لا بعده.

⚠ **الدمج في `master` ينشر إلى الإنتاج فورًا** — لا خطوة تجريب، وقاعدة
بيانات واحدة خلف كل شيء. ولا يوافق المالك على أي دمج حتى يكتمل النقل.

---

## ٦ · فخاخ تقنية أُمسِكت — لا تقع فيها ثانية

1. **`backdrop-filter` يصنع كتلة احتواء.** أيّ مرشِّح أو تحويل يجعل
   العنصر الأب مرجعًا لكل `position:fixed` بداخله. ركّب المبدّل خارج
   الرأسيتين، وافحص أي حاوية جديدة قبل التثبيت فيها.
2. **`\b` لا يتحقّق قبل `_`.** قيم Tailwind الحرّة تفصل الكلمات بشرطة
   سفلية، وهي محرف كلمة — فتدقيق `#[0-9a-f]{6}\b` **يفوّت** الألوان
   داخلها. بقي مشغّل الفيديو أزرق بسببها.
3. **`formatFils` تُقرأ آليًّا.** تمرّ من `Number()` في `ProductManager`
   وتُكتب في رسالة واتساب التي يُنسخ منها المبلغ إلى تطبيق المصرف.
   عرِّب عند العرض فقط.
4. **IBM Plex Mono بلا أرقام عربية.** كل ما يحمل رقمًا عربيًّا يجب أن
   يكون بخطّ الواجهة (`.numeric`)؛ والمونو لـ`.code` وحده.
5. **`1fr` حدّه الأدنى `min-content`.** استعمل `minmax(0,1fr)` في أي
   شبكة قد يطول محتواها.
6. **الأفقي المطلق يزيد `scrollWidth`.** و`overflow-x:hidden` يُخفي ولا
   يمنع، فتُزاح كل لقطة. `filter:blur` ينسكب مجّانًا.
7. **لا تقرأ اتجاه العربية من لقطة شاشة.** قِس مواضع المحارف
   بـ`Range.getBoundingClientRect`، تصاعديًّا لأرقام `direction:ltr`
   وتنازليًّا للنصّ العربي، واستبعد الشاشات المخفية (أبعادها صفر).
8. **الحركة ليست مدّتها فقط.** `stroke-dashoffset` و`height` و`top`
   و`box-shadow` المتحركة تعيد الرسم أو التخطيط ولو كانت بطيئة وناعمة.
   ثبّت الشكل، وحرّك `transform/opacity`، وأوقف الزخرفة خارج الإطار.
9. **تحويل التخطيط لا يضمن ترتيب التصيير.** لا تستعمل `!` على نتيجة
   جلسة داخل الصفحة بحجة أن التخطيط الأب فحصها؛ أعد الحارس في كل حدّ
   يقرأ القيمة، لأن App Router قد يصيّر الفروع بالتوازي.

---

## ٧ · الترتيب المقترح لمن يكمل

1. ~~**الحقول والمصادقة**~~ — **تمّت 2026-08-14.** وصدَق تقديرها: كشفت
   تسع نسخ يدوية وثلاث صور للتنبيه الواحد.
2. **الدرجات** — الجدول والحلقة والفارغ والهيكل؛ تخدم الطلبات
   والمستخدمين وطابور الطلبات أيضًا. ← **التالية**
3. **باقات صفحة المقرر** — `.tiers` ثلاثًا، وقوس الباقة.
4. **لوحة التحكم وبيئة الدراسة**.
5. **تفاصيل الهيكل** — `.navlink` والعدّاد وشريحة المستخدم واللوحة
   المنسحبة، مقارنةً بالمصدر لا بالذاكرة.

وبعد كل واحدة: البناء، والبصمة، والمِجَسّ، والفحص التسعة — وإعلان أي
انحراف **فورًا**، لا في تقرير مجمَّع.
